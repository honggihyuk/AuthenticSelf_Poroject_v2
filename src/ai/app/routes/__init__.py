"""FastAPI routers for the AI service.

Each router lives in its own sibling module — ``style.py`` (Task 4)
registers ``POST /analyze/style``. Additional routers land in
subsequent tasks. The root ``app.main`` module imports and mounts them
in order.
"""
