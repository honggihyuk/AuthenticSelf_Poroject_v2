# Mirror of [project.dependencies] in pyproject.toml — kept for tooling that
# still prefers requirements.txt (pip-compile, some CI). Source of truth is
# pyproject.toml; this file MUST stay in sync.
#
# AC-2: no ML-framework dependencies in this runtime — see pyproject.toml
# comment for the full policy. Only OpenCV + Pillow + numpy for image math.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
numpy>=1.26
opencv-python-headless>=4.9
pillow>=10.2

# dev-only — used by pytest suite under src/ai/tests
pytest>=8.0
httpx>=0.27
