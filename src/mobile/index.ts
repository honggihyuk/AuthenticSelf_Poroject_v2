import { registerRootComponent } from 'expo';
import App from './App';

// Expo-standard entry-point. Wraps the root component so it works both in
// Expo Go and in a standalone/bare build.
registerRootComponent(App);
